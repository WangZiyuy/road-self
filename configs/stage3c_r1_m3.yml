BASE_CONFIG: "stage3c_branch_aux.yml"

STAGE3C:
  OUTPUT_DIR: "data_self/stage3c_r1/m3"
  MATCHING:
    EXISTENCE_COST_WEIGHT: 1.0
  LOSS:
    EXIST_NO_OBJECT_COEF: 0.2
  MODEL:
    QUERY_SELF_ATTENTION_LAYERS: 0
  TRAINING:
    TRAJECTORY_MODALITY_DROPOUT: 0.0
  SANITY:
    SAMPLE_COUNT: 32
    GT_BRANCH_COUNT_MIN: 2
    GT_BRANCH_COUNT_MAX: 4
    MAX_EPOCHS: 600
    EVAL_EVERY_EPOCHS: 10
    DISABLE_MODEL_DROPOUT: True
    MIN_BRANCH_AP: 0.80
    MIN_EXACT_COUNT_ACCURACY: 0.80
    MIN_ORACLE_K_DISTINCT_GT_COVERAGE: 0.90
    MAX_ORACLE_K_DUPLICATE_PAIR_RATIO: 0.20
