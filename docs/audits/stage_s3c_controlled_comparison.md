# Stage S3C controlled comparison

The selected Phase A loss is `legacy_exact`. The R0-controlled common sample count is `40960`. The segmentation causal gate is `FAIL`. The frozen anchor gate is `NOT_EXECUTED_BY_GATE` and the graph stability gate is `NOT_EXECUTED_BY_GATE`.
